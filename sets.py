# set is a collection which is a collection of well defined objects.
s ={1, 5, 32, 54, 5, 5,5}

e = set() #empty set  s = {} it will create empty dictionary not set
# set do not allow duplicate values
print(s)
print(type(e)) # this will print the type of e which is set

#propeties of set:
#sets are unordered; elemnt's order doesn't matter.
#sets are mutable; we can change the content of a set after it has been created.
#sets are unindexed; cannot access elements by index
#There is no way to change itms in sets(immutable), but we can add or remove items from a set.
#sets cannot contain duplicate values
